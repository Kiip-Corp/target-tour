## Todo list
- 목업데이터를 API로 교체하기
(참고함- 데이터는 전부 목업 — fetchDemandMatrix() 함수와 NATION_DATA 객체 딱 두 군데에 몰아넣어놨음. 여기만 실제 API로 갈아끼우면 됨)

## Question
1. 각각의 데모 위에 데이터를 연동하면 될까요? 아니면 데모를 참고로 해서 새로 짜야하나요?
2. 콘텐츠 생성은 클로드 API를 호출중 -> ANTHROPIC_API_KEY가 필요합니다